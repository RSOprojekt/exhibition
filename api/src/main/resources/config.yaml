kumuluzee:
  name: exhibition-service
  env:
    name: dev
  version: 1.0.0
  server:
    base-url: http://localhost:8080
    http:
      port: 8080
  datasources:
    - jndi-name: jdbc/ExhibitionMetadataDS
      connection-url: jdbc:postgresql://localhost:5432/exhibition-metadata
      username: dbuser
      password: postgres
      max-pool-size: 20
